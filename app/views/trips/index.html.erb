<h1>投稿一覧</h1>
<% @tag_lists.each do |tag| %>
  <span>
  <%= link_to tag.name, tag_trips_path(tag_id: tag.id) %>
  <%= tag.trips.count %>
  </sapn>
<% end %>
<%= render 'index', trips: @trips, tag_lists: @tag_lists %>